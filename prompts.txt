Fix a "document is not defined" SSR crash traced through @floating-ui/react-dom (used by Radix UI dropdowns/popovers/tooltips).

This is likely caused by a component from the recent UI redesign (possibly the relocated language changer, or another new/changed component) that uses a floating-UI-based element (Select, DropdownMenu, Popover, Tooltip) in a way that executes during server-side rendering instead of being properly client-only.

1. Identify which recently-changed component is causing this. Check the language changer component specifically first (since it was just moved to Settings), and any other new components from the UI redesign.

2. Common causes and fixes:
   - A component accessing `document` directly in the render body (not inside useEffect) — move any such access into a useEffect hook so it only runs client-side after hydration
   - A Radix UI component (Select, DropdownMenu, etc.) being rendered unconditionally during SSR when it should only render once mounted client-side — wrap it with a mounted-state check (const [mounted, setMounted] = useState(false); useEffect(() => setMounted(true), []); return mounted ? <Component /> : null; or similar) if the component genuinely can't SSR safely
   - Check if a `document.getElementById`, `document.querySelector`, or similar call was added directly in a component body rather than in an effect

3. Search specifically:
   Get-ChildItem -Path src -Include *.ts,*.tsx -Recurse | Select-String -Pattern "document\."

MANDATORY VERIFICATION:
- Run npm run build
- Run npx wrangler dev --config wrangler.json (NOT --local)
- Load the homepage — confirm NO "document is not defined" error and the page actually renders
- Navigate through several pages (dashboard, settings, plans) to confirm the fix isn't narrowly scoped to just the homepage

Paste actual terminal output showing a clean load across multiple pages.

Do NOT touch voice code, tool-call loop, or subscription/Stripe work.